# 渐开线花键参数计算工具 — Skill 定义

## 1. 功能描述

为电机工程师提供符合 **GB/T 3478.1-2008《圆柱直齿渐开线花键（米制模数 齿侧配合）》** 的渐开线花键全参数计算工具。用户在浏览器中打开单页 HTML，输入模数、齿数、公差配合等参数，即可获得分度圆、基圆、大径小径极限值、齿厚/齿槽宽公差带、跨棒距/棒间距、配合侧隙等完整工程参数。

强度校核按《机械设计手册》第五版执行 **7 项全面校核**：齿面接触、齿根弯曲、齿根抗剪、10⁸循环磨损、长期无磨损、轴扭转、轴弯曲。

核心目标：**计算准确性绝对优先**，所有公式标注标准编号，中间变量完整保留、支持人工逐行验算。

---

## 2. 参考标准

| 标准编号 | 名称 | 适用范围 |
|----------|------|----------|
| **GB/T 3478.1-2008** | 圆柱直齿渐开线花键 第1部分：总论 | 基本齿廓、公差体系、配合类别 |
| **GB/T 3478.5-2008** | 圆柱直齿渐开线花键 第5部分：检验 | 量棒测量（跨棒距/棒间距）公式 |
| **《机械设计手册》第五版 第3卷** | 花键联接强度计算 | 齿面挤压、齿根弯曲、齿根抗剪校核 |
| **《机械设计手册》第五版 第3卷** | 磨损寿命评估 | p·v 值法 — 10⁸循环磨损 / 长期无磨损校核 |
| **《机械设计手册》第五版 第2卷** | 轴的设计计算 | 外花键轴扭转、弯扭合成强度校核 |

---

## 3. 输入参数

| 参数 | 符号 | 单位 | 说明 |
|------|------|------|------|
| 模数 | `m` | mm | 标准系列：0.5, 0.75, 1, 1.25, 1.5, 2, 2.5, 3, 4, 5, 6, 8, 10 |
| 齿数 | `z` | — | 范围 6~120 |
| 公差等级 | — | — | 4级(精密磨削) / 5级(磨削/精拉) / 6级(拉削/滚齿) / 7级(一般机加) |
| 配合类别 | — | — | H/h(滑动) / H/f(紧滑动) / H/e(转动) / H/d(松转动) / H/k(过渡) / H/js(过渡对称) |
| 齿根形式 | — | — | 平齿根(flatRoot) / 圆齿根(filletRoot) |
| 量棒直径 | D_R | mm | 0=自动推荐(推荐值≈1.732m，就近取 R40 标准量棒) |
| 配合长度 | L | mm | 0=自动取分度圆直径 D |
| **— 强度校核参数（可选，不填则跳过） —** | | | |
| 花键材料 | — | — | 45钢调质 / 40Cr调质 / 42CrMo调质 / 20CrMnTi渗碳淬火 / 38CrMoAl氮化 |
| 传递转矩 | T | N·m | 直接输入（与功率/转速二选一） |
| 功率 | P | kW | 与转速配合自动推算 T=9550·P/n |
| 转速 | n | rpm | 用于转矩推算 + 磨损校核的滑动速度计算 |
| 轴上弯矩 | M | N·m | 可选，用于轴弯曲弯扭合成校核 |

---

## 4. 计算公式（逐步骤）

### 4.1 基本几何（GB/T 3478.1-2008 第5章）

```
① 分度圆直径      D = m × z                                              (mm)
② 基圆直径        Db = D × cos(30°) = m × z × 0.8660254                 (mm)
③ 齿距            p = π × m                                              (mm)
④ 基本齿厚/齿槽宽  S = E = p / 2 = π × m / 2                            (mm)
⑤ 齿顶高          ha = 0.5 × m                                           (mm)
⑥ inv(30°)       = tan30° - π/6 = 0.0537515
```

### 4.2 外花键（轴）直径（GB/T 3478.1-2008 第6章 + 表24/25）

```
⑦ 大径(基本)      D_ee = m × (z + 1)                                     (mm)
⑧ 小径(基本)      D_ie = m × (z - 1.5)  [平齿根]                         (mm)
                  D_ie = m × (z - 1.8)  [圆齿根]                         (mm)
⑨ 大径上偏差      = es_v/tanα_D — 从表24（30°压力角列）查取 (μm)
                   f配合: -17~-149, e配合: -35~-320, d配合: -52~-554
⑩ 大径公差        = 从表25直接查表 (μm)
                   m=0.25~0.75: 48~120, m=1~1.75: 90~250, m=2~10: 180~900
    小径上偏差同大径（表24），小径公差同大径（表25）
```

### 4.3 内花键（毂）直径（GB/T 3478.1-2008 第6章 + 表25）

```
⑪ 大径(基本)      D_ei = m × (z + 1.5)  [平齿根]                         (mm)
                  D_ei = m × (z + 1.8)  [圆齿根]                         (mm)
⑫ 小径(基本)      D_ii = m × (z - 1)                                     (mm)
⑬ 大径公差        IT12~IT14（按模数分段，IT公式回退）
                   下偏差=0(H偏差)，公差值通过 IT 乘数公式计算
⑭ 小径上偏差      = 从表25直接查表 (μm) — H偏差，下偏差=0
                   m=0.25~0.75: +48~+120, m=1~1.75: +90~+250, m=2~10: +180~+900
```

### 4.4 公差系统（GB/T 3478.1-2008 第8章）

```
⑮ 公差单位 i*(D)  = 0.45 × ∛D + 0.001 × D         (D≤500mm)           (μm)
⑯ 公差单位 i**(S) = 0.45 × ∛S + 0.001 × S                            (μm)
⑰ 总公差 (T+λ)    = K1 × i* + K2 × i**
     K1/K2 系数: 4级=10/40, 5级=16/64, 6级=25/100, 7级=40/160        (μm)

⑱ 齿距累积公差 Fp = fpA × √L_arc + fpB       (L_arc=π·m·z/2)        (μm)
    4级: 2.5√L_arc+6.3,  5级: 3.55√L_arc+9
    6级: 5.0√L_arc+12.5, 7级: 7.1√L_arc+18   — GB/T 3478.1 第8.4节

⑲ 齿形公差 ff = ffA × φ + ffB       (φ=m+0.0125·m·z, 公差因数)      (μm)
    4级: 1.6φ+10,  5级: 2.5φ+16
    6级: 4.0φ+25,  7级: 6.3φ+40        — GB/T 3478.1 第8.5节

⑳ 齿向公差 Fβ = fbetaA × √g + fbetaB      (g=配合长度L_eng)         (μm)
    4级: 0.8√g+4,   5级: 1.0√g+5
    6级: 1.25√g+6.3, 7级: 2.0√g+10      — GB/T 3478.1 第8.6节

㉑ 综合公差 λ = 0.6 × √(Fp² + ff² + Fβ²)                              (μm)
㉒ 加工公差 T = (T+λ) - λ                                              (μm)
```

### 4.5 齿厚与齿槽宽极限

```
㉓ 外花键作用齿厚上偏差 es_v — GB/T 3478.1-2008 表23 查表 (μm):
    按分度圆直径 D 分段 (≤6, >6~10, ... >800~1000):
    h: es_v = 0
    js: es_v = +(T+λ)/2
    k: es_v = +(T+λ)
    f: -10 到 -86（分段递减，如 D>50~80: -30）
    e: -20 到 -170（分段递减，如 D>50~80: -60）
    d: -30 到 -320（分段递减，如 D>50~80: -100）

    外花键大径/小径上偏差 = es_v/tanα_D — 表24（30°列）查表 (μm)

㉔ 外花键实际齿厚:
    S_max = S_basic + es_v/1000 - λ/1000                               (mm)
    S_min = S_basic + es_v/1000 - (T+λ)/1000                           (mm)

㉕ 内花键实际齿槽宽 (基孔制 H, EI_v=0):
    E_min = E_basic + λ/1000                                           (mm)
    E_max = E_basic + (T+λ)/1000                                       (mm)

㉖ 配合侧隙:
    最大侧隙 = E_max - S_min                                           (mm)
    最小侧隙 = E_min - S_max                                           (mm)
```

### 4.6 量棒测量（GB/T 3478.5-2008）

```
㉗ 外花键跨棒距 M_Re:
    inv(α_e) = S/D + inv(30°) + D_R/D_b - π/z
    α_e = inverseInvolute(inv(α_e))          [牛顿迭代求解]
    M_Re = Db/cos(α_e) + D_R                (偶齿数)
    M_Re = Db/cos(α_e)·cos(90°/z) + D_R     (奇齿数)

㉘ 内花键棒间距 M_Ri:
    inv(α_i) = E/D + inv(30°) - D_R/D_b
    α_i = inverseInvolute(inv(α_i))
    M_Ri = Db/cos(α_i) - D_R                (偶齿数)
    M_Ri = Db/cos(α_i)·cos(90°/z) - D_R     (奇齿数)

㉙ 推荐量棒直径: D_R ≈ 1.732m (30°压力角理论值)
    实际选取: 最接近理论值的 R40 优先数系标准量棒
```

### 4.7 强度校核（《机械设计手册》第五版 — 7 项）

#### ㉚ 转矩推算（当转矩未直接输入时）
```
T = 9550 × P / n                                   (N·m)
式中: P — 功率(kW), n — 转速(rpm)
```

#### ㉛ 齿面接触（挤压）强度 — 第3卷 花键联接
```
σ_H = 2000·T / (ψ·z·h·l·D_m)                                          (MPa)
式中: ψ=0.75(载荷不均系数), h=(D_ee-D_ii)/2(接触高度,mm),
      l=配合长度(mm), D_m≈D(平均直径,mm)
判定: n_H = [σ_H] / σ_H ≥ 1.5（通用工业），≥1.8（新能源），≥2.0（高速），≥2.5（航空）
```

#### ㉜ 齿根弯曲强度 — 第3卷 花键联接
```
σ_F = 6000·T·h / (ψ·z·S_fn²·l·D_m)                                    (MPa)
式中: S_fn≈πm/2(齿根弦齿厚,mm), h=(D_ee-D_ie)/2(全齿高,mm)
判定: n_F = [σ_F] / σ_F ≥ 1.3（通用工业），≥1.8（新能源/高速/航空）
```

#### ㉝ 齿根抗剪强度 — 第3卷 花键联接
```
τ = 2000·T / (ψ·z·S_fn·l·D_m)                                          (MPa)
式中: 切向力 F_t=2000T/D_m, 剪切面积 A=ψ·z·S_fn·l
判定: n_τ = [τ] / τ ≥ 1.3（通用工业电机）
```

#### ㉞ 10⁸ 循环磨损校核 — 第3卷 p·v 值法
```
v_s = π·h·n / 60000                                                     (m/s)
p·v = σ_H × v_s                                                         (MPa·m/s)
式中: v_s — 齿面平均滑动速度, h — 接触高度(mm), n — 转速(rpm)
10⁸循环总滑动距离: L_s = v_s × (10⁸/n/60) × 3600                       (m)
判定: n_pv = [p·v] / (p·v) ≥ 1.5
```

#### ㉟ 长期工作无磨损校核 — 第3卷 微动磨损门槛
```
σ_H ≤ [σ_Hw]                                                             (MPa)
式中: [σ_Hw] — 材料无磨损许用接触应力，取决于材料与硬度
      σ_H 低于此门槛值时，即使长期交变载荷也不发生显著微动磨损
适用工况: 长寿命(>10⁹循环)、维护困难、要求零磨损的场合
```

#### ㊱ 外花键轴扭转强度 — 第2卷 轴的设计
```
τ_max = T / W_t = 16000·T / (π·D_ie_min³)                               (MPa)
式中: W_t = π·d³/16（实心轴抗扭截面模量）, d = D_ie_min（外花键小径最小值）
判定: n_τ = [τ] / τ_max ≥ 1.3
```

#### ㊲ 外花键轴弯曲强度（弯扭合成） — 第2卷 轴的设计
```
σ_b = M / W_b = 32000·M / (π·D_ie_min³)                                 (MPa)
σ_e = √(σ_b² + 3·τ_max²)              [第四强度理论，弯扭合成应力]        (MPa)
式中: W_b = π·d³/32（实心轴抗弯截面模量）, M — 轴上弯矩(N·m)
判定: n_e = [σ] / σ_e ≥ 1.3
注: 未提供弯矩值时跳过此项校核，仅输出截面模量供参考
```

---

## 5. 安全系数判定标准

### 5.1 齿面接触与齿根弯曲（按行业分级，内部判定）

| 行业场景 | 接触强度 n_H | 弯曲强度 n_F | 抗剪强度 n_τ | 依据 |
|----------|:---:|:---:|:---:|------|
| 通用工业电机 | ≥1.5 | ≥1.3 | ≥1.3 | 《机械设计手册》第五版 |
| 新能源汽车驱动电机 | ≥1.8 | ≥1.8 | ≥1.8 | GB/T 18488-2024 |
| 高速永磁电机 (≥10000rpm) | ≥2.0 | ≥2.0 | ≥2.0 | 高速电机设计规范 |
| 航空级电机 (eVTOL) | ≥2.5 | ≥2.5 | ≥2.5 | 航空级失效概率要求 |

> **注意**：当前界面简化为统一使用「通用工业电机」标准（n_H≥1.5, n_F≥1.3），行业场景判定在 calc.js 内部的 `SAFETY_STANDARDS` 中保留，可通过 `industry` 参数切换。

### 5.2 磨损与轴校核（统一标准）

| 校核类型 | 安全系数 | 说明 |
|----------|:---:|------|
| 10⁸循环磨损 p·v | ≥1.5 | p·v 值法，确保 10⁸次循环磨损量在许用范围内 |
| 长期无磨损 | 裕度 m≥1.0 | σ_H ≤ [σ_Hw] 即满足，m = [σ_Hw]/σ_H |
| 轴扭转 | ≥1.3 | 同通用工业弯曲标准 |
| 轴弯曲（弯扭合成）| ≥1.3 | 第四强度理论，仅在有弯矩输入时校核 |

---

## 6. 材料许用值

5 种常用花键材料，每种材料含 7 个许用参数：

| 材料 | [σ_H] MPa | [σ_F] MPa | [τ] MPa | [p·v] MPa·m/s | [σ_Hw] MPa | 硬度 |
|------|:---:|:---:|:---:|:---:|:---:|------|
| 45钢调质 | 100 | 120 | 60 | 2.5 | 45 | HB220-250 |
| 40Cr调质 | 140 | 170 | 85 | 3.5 | 60 | HB250-280 |
| 42CrMo调质 | 150 | 180 | 90 | 4.0 | 70 | HB260-300 |
| 20CrMnTi渗碳淬火 | 200 | 250 | 120 | 8.0 | 100 | HRC58-62 |
| 38CrMoAl氮化 | 180 | 220 | 110 | 6.0 | 90 | HV900-1100 |

---

## 7. UI 要求

### 7.1 布局
- **左侧固定面板**（380px）：参数输入区，sticky 定位。分为基本参数区 + 强度校核参数区（折叠可选）
- **右侧自适应区域**：结果展示，按卡片分组（6张卡片）
- 响应式：窄屏时上下堆叠

### 7.2 结果卡片
| 卡片 | 内容 |
|------|------|
| 📐 基本参数 | 分度圆、基圆、齿距、齿厚、配合类别、侧隙范围 |
| 🔧 外花键（轴） | 大径/小径（机械制图公差标注 `φ52 0/-0.173`）、渐开线起始圆、齿厚范围、跨棒距 |
| 🔩 内花键（毂） | 大径/小径（公差标注 `φ53 +0.279/0`）、渐开线终止圆、齿槽宽范围、棒间距 |
| 📏 公差明细 | (T+λ)、λ、T、Fp、ff、Fβ 及计算式 |
| 🛡️ 强度校核 | 7 项校核结果（仅在有转矩输入时显示）|
| 📝 中间计算详情 | 默认折叠，逐步骤公式代入（步骤①~⑯），供人工验算 |

### 7.3 显示规范
- **直径公差**：使用机械制图标注法（`φ52 0/-0.173`），不拆分基本值/max/min 三行
- **齿厚/齿槽宽**：显示为 `3.080 / 3.033`（max/min），附带公差带信息
- **状态标签**：绿色(合格/无磨损) / 黄色(警告/可能磨损) / 红色(不合格)

### 7.4 视觉风格
- **配色**：深蓝灰主色调 (`#1a3a5c`)，橙色点缀 (`#e67e22`)
- **字体**：中文 PingFang SC / Microsoft YaHei，数字等宽字体 Consolas / SF Mono
- **卡片化**：圆角 6px，浅阴影，明确分组
- **CSS 变量体系**：统一在 `:root` 中定义

### 7.5 交互
- 所有结果卡片可**点击标题折叠/展开**
- 底部提供**"中间计算详情"**展开区，展示逐步骤公式代入过程
- 预设**快速示例按钮** ×5（GB/T 3478.1-2008 附录C：C.1~C.5）+ ×1（GB/T 17855-1999 例1：强度校核）
- 量棒直径根据模数**自动推荐**，也可手动输入
- 配合长度留空时自动取 1×D
- 转矩可通过直接输入或功率+转速自动推算（T=9550·P/n）

### 7.6 打印
- `@media print` 隐藏输入面板
- 仅保留计算结果卡片

---

## 8. 文件结构强制规范

```
spline-calc/
├── data.js        # 数据层 — 只放参数表、系数常量，无计算逻辑
│   ├── BASIC_PROFILE_30        # 30°基本齿廓常量（平/圆齿根系数）
│   ├── MODULE_SERIES           # 标准模数系列（第1/第2优先）
│   ├── TOLERANCE_GRADES        # 4/5/6/7级公差系数 (K1/K2, Fp/ff/Fβ)
│   ├── FIT_TYPES               # 6种配合类别 + es_v 公式类型
│   ├── getRecommendedPinDiameter()  # 量棒推荐（理论 1.732m → R40 标准）
│   ├── SAFETY_STANDARDS        # 4种行业判定标准（n_H, n_F 阈值）
│   └── MATERIAL_PROPERTIES     # 5种材料 × 7项许用参数
│
├── calc.js        # 计算层 — 纯函数，无DOM操作（～30个函数）
│   ├── involute() / inverseInvolute()    # 渐开线函数 / 牛顿反求
│   ├── calcBasicGeometry()               # ①~⑥ 基本几何
│   ├── calcExternalSplineDiameters()     # ⑦~⑩ 外花键直径
│   ├── calcInternalSplineDiameters()     # ⑪~⑭ 内花键直径
│   ├── calcExternalFormDiameter()        # 渐开线起始圆
│   ├── calcInternalFormDiameter()        # 渐开线终止圆
│   ├── calcToleranceUnit_D() / _S()      # ⑮~⑯ 公差单位
│   ├── calcTotalTolerance()              # ⑰ 总公差
│   ├── calcComprehensiveTolerance()      # ⑱~㉒ 综合公差
│   ├── calcFundamentalDeviation()        # ㉓ 基本偏差 es_v
│   ├── calcToothThicknessLimits()        # ㉔ 齿厚极限
│   ├── calcSpaceWidthLimits()            # ㉕ 齿槽宽极限
│   ├── calcMeasurementOverPins()         # ㉗ 跨棒距 M_Re
│   ├── calcMeasurementBetweenPins()      # ㉘ 棒间距 M_Ri
│   ├── calcDiameterTolerance()           # IT 公差计算
│   ├── calcTorqueFromPower()             # ㉚ 功率→转矩 T=9550P/n
│   ├── checkContactStrength()            # ㉛ 齿面接触（挤压）
│   ├── checkBendingStrength()            # ㉜ 齿根弯曲
│   ├── checkShearStrength()              # ㉝ 齿根抗剪
│   ├── checkWear10e8()                   # ㉞ 10⁸循环磨损 (p·v)
│   ├── checkWearFreeLongTerm()           # ㉟ 长期无磨损
│   ├── checkShaftTorsion()               # ㊱ 轴扭转
│   ├── checkShaftBending()               # ㊲ 轴弯曲（弯扭合成）
│   ├── evaluateStrength() / evaluateCheck()  # 通用判定函数
│   └── calcAll()                         # 综合计算入口（串联全部子函数）
│
└── index.html     # 界面层 — 布局+交互，所有计算调用calc.js
    ├── 输入面板（模数/齿数/公差等级/配合/齿根/量棒/配合长度/
    │            材料/转矩/功率/转速/弯矩）
    ├── 结果卡片区（6张卡片，含7项强度校核）
    ├── 快捷示例按钮 ×5（GB/T 3478.1-2008 附录 C）
    └── JavaScript 桥接层（数据收集 → calcAll() → 渲染）
```

---

## 9. 红线禁令

1. **禁止** 在 `index.html` 中编写任何计算公式
2. **禁止** 在 `calc.js` 中操作 DOM 或引用界面元素
3. **禁止** 在 `data.js` 中包含任何计算逻辑
4. **禁止** 合并计算步骤、省略中间变量
5. **禁止** 修改国标公式的系数、修正项而不标注来源
6. **禁止** 统一使用单一安全系数阈值 — 接触/弯曲/剪切/磨损/轴校核各有独立判定标准
7. **禁止** 忽略转速、材料等电机工况参数的影响 — 磨损校核依赖转速计算滑动速度
8. **禁止** 直径显示拆分为 basic/max/min 三行 — 统一使用机械制图公差标注

---

## 10. 验证用例（GB/T 3478.1-2008 附录 C）

以下 5 个标准用例均使用 m=1.0, z=25, L=12.5mm(=D/2), 量棒自动推荐, 转矩=0(跳过强度校核)。

### C.1 — INT 25z×1.0m×30P×5H（内花键 平齿根 5级 H配合）
```
输入: m=1, z=25, 5级, H/h, 平齿根, L=12.5mm
期望输出:
  D=25.00, Db=21.65064
  内花键: D_ei=φ26.50 (+0.21/0 IT12), D_ii=φ24.09 (+0.13/0 H11)
  齿槽宽: E_max=1.626, E_min=1.594, E_vmin=1.571
  公差: T+λ=55μm, λ=23μm, T=32μm
    Fp=31μm (3.55√39.27+9), ff=19μm (2.5×1.3125+16), Fβ=9μm (1.0√12.5+5)
  量棒 D_Ri=1.80, 棒间距: M_Ri=22.324/22.260 (max/min)
```

### C.2 — INT 25z×1.0m×30R×7H（内花键 圆齿根 7级 H配合）
```
输入: m=1, z=25, 7级, H/h, 圆齿根, L=12.5mm
期望输出:
  D_ei=φ26.80 (+0.21/0 IT12)  ← 圆齿根, 比C.1大0.3mm
  齿槽宽: E_max=1.709, E_min=1.619
  公差: T+λ=138μm, λ=48μm
    Fp=62μm (7.1√39.27+18), ff=48μm (6.3×1.3125+40), Fβ=17μm (2.0√12.5+10)
  量棒 D_Ri=1.90, 棒间距: M_Ri=22.161/21.981 (max/min)
```

### C.3 — EXT 25z×1.0m×30R×4h（外花键 圆齿根 4级 h配合）
```
输入: m=1, z=25, 4级, H/h, 圆齿根, L=12.5mm
期望输出:
  D_ee=φ26.00 (0/-130μm IT11), D_ie=φ23.20 (0/-210μm IT12)
  es_v=0 (h配合, 表23), 大径/小径上偏差=0
  齿厚: S_max=1.555, S_min=1.537, S_vmax=1.571
  公差: T+λ=34μm, λ=16μm
    Fp=22μm (2.5√39.27+6.3), ff=12μm (1.6×1.3125+10), Fβ=7μm (0.8√12.5+4)
  量棒 D_Re=2.00, 跨棒距: M_Re=28.124/28.095 (max/min)
```

### C.4 — EXT 25z×1.0m×30R×6e（外花键 圆齿根 6级 e配合）
```
输入: m=1, z=25, 6级, H/e, 圆齿根, L=12.5mm
期望输出:
  es_v=-40μm (表23, D=25→>18~30段), es_v/tanα_D=-69μm (表24, 30°列)
  D_ee max=25.931 (26.00-0.069), D_ee min=25.801 (IT11)
  D_ie max=23.131 (23.20-0.069), D_ie min=22.921 (IT12)
  齿厚: S_max=1.498, S_min=1.445, S_vmax=1.531
  公差: T+λ=86μm, λ=33μm
    Fp=44μm (5.0√39.27+12.5), ff=30μm (4.0×1.3125+25), Fβ=11μm (1.25√12.5+6.3)
  量棒 D_Re=2.00, 跨棒距: M_Re=28.036/27.954 (max/min)
  公法线(K=5): W_min=13.298, W_max=13.344
```

### C.5 — EXT 25z×1.0m×30P×5js（外花键 平齿根 5级 js配合）
```
输入: m=1, z=25, 5级, H/js, 平齿根, L=12.5mm
期望输出:
  es_v=+(T+λ)/2=+27.5μm (js配合, 表23)
  es_v/tanα_D=+48μm (表24, 30°列)
  D_ee max=26.00, D_ee min=25.870 (IT11)
  D_ie max=23.548 (平齿根 23.50+0.048), D_ie min=23.338 (IT12)
  齿厚: S_max=1.575, S_min=1.543, S_vmax=1.598
  公差: T+λ=55μm, λ=23μm (同C.1, 同为5级)
  量棒 D_Re=2.00, 跨棒距: M_Re=28.155/28.105 (max/min)
  公法线(K=5): W_min=13.383, W_max=13.410
```
